openapi: 3.1.2
info:
  title: TerminPilot API
  version: 0.0.0
servers:
  - url: http://localhost:8080
    description: Local Development
tags:
  - name: Calendar
    description: Operations related to a user's calendar api
paths:
  /api/calendar/events:
    $ref: './paths/events.yaml'