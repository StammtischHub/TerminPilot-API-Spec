openapi: 3.1.2
info:
  title: TerminPilot API
  version: 0.0.0

servers:
  - url: http://localhost:8080
    description: Local Development

paths:
  /termine:
    $ref: './paths/termine.yaml'

components:
  schemas:
    Termin:
      $ref: './schemas/termin.yaml#/Termin'
    CreateTerminRequest:
      $ref: './schemas/termin.yaml#/CreateTerminRequest'
