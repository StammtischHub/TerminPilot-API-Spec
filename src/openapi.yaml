openapi: 3.1.2
info:
  title: TerminPilot API
  version: 1.0.2

servers:
  - url: http://localhost:8080
    description: Local Development

tags:
  - name: Auth
    description: Endpoints for authentication
  - name: Calendar Connections
    description: Endpoints for connected external calendars

paths:
  /api/auth/register:
    $ref: './paths/auth/register.yaml'

  /api/auth/login:
    $ref: './paths/auth/login.yaml'

  /api/auth/logout:
    $ref: './paths/auth/logout.yaml'

  /api/auth/me:
    $ref: './paths/auth/me.yaml'

  /api/calendar-connections/{connectionId}/calendars/{calendarId}/events:
    $ref: './paths/calendar-connections/create-calendar-event.yaml'
