type: object
required: [event, participants]
properties:
  constraints:
    $ref: '../Event.yaml'
  participants:
    type: array
    minItems: 1
    items:
      participantId:
        type: integer
        format: int64
        example: 1234
